{
  "name": "findmyclass",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "build": "npm ci --prefix server && npm ci --prefix client && npm run build --prefix client",
    "start": "npm run start:production --prefix server",
    "test": "npm test --prefix server && npm run build --prefix client"
  }
}
